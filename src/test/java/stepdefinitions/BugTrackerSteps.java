package stepdefinitions;

import io.cucumber.java.After;
import io.cucumber.java.Before;
import io.cucumber.java.en.And;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;
import org.junit.Assert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import pages.BugPage;
import pages.LoginPage;

import java.time.Duration;

/**
 * Step definitions for the bug tracking application using Selenium WebDriver,
 * Cucumber and JUnit 4. These steps implement a page object model and
 * encapsulate interactions with the LoginPage and BugPage classes.
 */
public class BugTrackerSteps {



}